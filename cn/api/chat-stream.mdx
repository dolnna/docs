---
title: '聊天（流式）—— POST /product/chat/stream'
description: '发送消息并以 SSE 流的形式接收带记忆增强的回复。Token 在生成时即到达，您的 UI 可以实时更新。'
---

chat stream 端点运行与 [chat complete](/cn/api/chat-complete) 相同的带记忆增强的流水线 —— 检索相关记忆、构建上下文丰富的系统提示、并将交互写回记忆 —— 但通过 Server-Sent Events (SSE) 逐 token 流式输出 LLM 的输出。每个事件都携带一个小型 JSON 负载，使您的客户端可以渐进式地呈现部分响应。在交互式聊天 UI、语音界面或任何对感知延迟敏感的集成中使用此端点。如果您只需要完成后的响应且不想处理 SSE 流，请改用 [POST /product/chat/complete](/cn/api/chat-complete)。

<Note>
  此端点要求在您的环境中设置 `ENABLE_CHAT_API=true`。如果聊天服务未初始化，服务器返回 `503` 错误。
</Note>

## 端点

```
POST /product/chat/stream
```

## 请求体

<ParamField body="user_id" type="string" required>
  用户的唯一标识符。MemOS 使用它限定记忆检索和存储的范围。
</ParamField>

<ParamField body="query" type="string" required>
  用户的消息或问题。
</ParamField>

<ParamField body="readable_cube_ids" type="string[]">
  用于检索上下文的 MemCube ID 列表。省略时，MemOS 回退到用户的默认 cube。
</ParamField>

<ParamField body="writable_cube_ids" type="string[]">
  用于写入新交互的 MemCube ID 列表。省略时，MemOS 回退到用户的默认 cube。
</ParamField>

<ParamField body="session_id" type="string">
  可选的会话标识符。用作软信号以浮现与当前对话相关的记忆。不充当硬过滤器。
</ParamField>

<ParamField body="history" type="object[]">
  要包含在 LLM 上下文窗口中的先前对话轮次。最多使用最后 20 轮。

  <Expandable title="message object">
    <ParamField body="history[].role" type="string" required>
      说话者的角色。`"user"` 或 `"assistant"` 之一。
    </ParamField>
    <ParamField body="history[].content" type="string" required>
      消息的文本内容。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="mode" type="string" default="fast">
  记忆检索模式。`"fast"` 运行单遍向量搜索。`"fine"` 运行更深入的多遍搜索以获得更高召回率，代价是延迟。`"mixture"` 结合两种策略。
</ParamField>

<ParamField body="top_k" type="integer" default="10">
  检索并注入提示的最大文本记忆数。
</ParamField>

<ParamField body="relativity" type="number" default="0.45">
  记忆被包含的最低相关性分数。设置为 `0` 以禁用阈值过滤。
</ParamField>

<ParamField body="include_preference" type="boolean" default="true">
  当为 `true` 时，MemOS 检索偏好记忆并将其附加到系统提示。
</ParamField>

<ParamField body="pref_top_k" type="integer" default="6">
  要检索的最大偏好记忆数。
</ParamField>

<ParamField body="system_prompt" type="string">
  在记忆上下文之前预置的基础系统提示。省略时，MemOS 使用其内置的提示模板。
</ParamField>

<ParamField body="model_name_or_path" type="string">
  要使用的 LLM 名称。必须匹配您的服务器配置中已注册的某个模型。省略时，使用服务器的默认模型。
</ParamField>

<ParamField body="max_tokens" type="integer">
  要生成的最大 token 数。省略时，应用模型默认值。
</ParamField>

<ParamField body="temperature" type="number">
  采样温度。省略时，应用模型默认值。
</ParamField>

<ParamField body="top_p" type="number">
  核采样参数。省略时，应用模型默认值。
</ParamField>

<ParamField body="add_message_on_answer" type="boolean" default="true">
  当为 `true` 时，MemOS 在流结束后异步将完整交互写入记忆。设置为 `false` 以跳过写回步骤。
</ParamField>

<ParamField body="filter" type="object">
  应用于记忆检索的结构化过滤器。支持 `"and"` 和 `"or"` 条件。

  <Expandable title="filter example">
    ```json
    {
      "and": [
        { "created_at": { "gt": "2024-01-01" } }
      ]
    }
    ```
  </Expandable>
</ParamField>

<ParamField body="internet_search" type="boolean" default="false">
  当为 `true` 时，使用实时互联网搜索增强记忆检索。
</ParamField>

## 响应格式

响应是一个连续的 `text/event-stream` (SSE) 流。每个事件是以 `data: ` 开头并跟随一个 JSON 对象的行，事件之间用空白行分隔。

### 事件类型

<ResponseField name="type" type="string" required>
  标识事件类型。可能的值：

  - `"text"` —— 助手回复的 token 或片段。按顺序拼接所有 `"text"` 负载以重构完整响应。
  - `"reasoning"` —— 来自 `<think>…</think>` 块内的思维链片段。仅当模型输出推理 token 时存在。推理事件先于对应的 `"text"` 事件。
  - `"error"` —— 描述流式过程中发生错误的字符串。此事件后连接关闭。
</ResponseField>

<ResponseField name="data" type="string" required>
  此事件片段的内容。
</ResponseField>

**SSE 流示例**

```
data: {"type": "text", "data": "You"}

data: {"type": "text", "data": " mentioned"}

data: {"type": "text", "data": " that you prefer"}

data: {"type": "text", "data": " window seats on flights."}
```

## 示例

### curl

```bash cURL
curl --request POST \
  --url http://localhost:8000/product/chat/stream \
  --header 'Content-Type: application/json' \
  --no-buffer \
  --data '{
    "user_id": "8736b16e-1d20-4163-980b-a5063c3facdc",
    "query": "What are my travel preferences?",
    "readable_cube_ids": ["b32d0977-435d-4828-a86f-4f47f8b55bca"],
    "writable_cube_ids": ["b32d0977-435d-4828-a86f-4f47f8b55bca"],
    "session_id": "session-abc-123"
  }'
```

`-N` / `--no-buffer` 标志禁用 curl 的输出缓冲，让您在每个事件到达时立即看到它。

### Python —— 消费 SSE 流

```python Python
import json
import requests

response = requests.post(
    "http://localhost:8000/product/chat/stream",
    json={
        "user_id": "8736b16e-1d20-4163-980b-a5063c3facdc",
        "query": "What are my travel preferences?",
        "readable_cube_ids": ["b32d0977-435d-4828-a86f-4f47f8b55bca"],
        "writable_cube_ids": ["b32d0977-435d-4828-a86f-4f47f8b55bca"],
        "session_id": "session-abc-123",
    },
    stream=True,
)

full_response = ""
for line in response.iter_lines():
    if not line:
        continue
    # Each line is bytes; decode and strip the "data: " prefix
    text = line.decode("utf-8")
    if not text.startswith("data: "):
        continue
    event = json.loads(text[len("data: "):])
    if event["type"] == "text":
        full_response += event["data"]
        print(event["data"], end="", flush=True)
    elif event["type"] == "error":
        print(f"\nStream error: {event['data']}")
        break

print()  # newline after stream ends
print("Full response:", full_response)
```

### Python —— 使用 sseclient

如果您更喜欢更高层级的 SSE 库，请安装 `sseclient-py` 并按如下方式使用：

```python Python
import json
import requests
import sseclient

response = requests.post(
    "http://localhost:8000/product/chat/stream",
    json={
        "user_id": "8736b16e-1d20-4163-980b-a5063c3facdc",
        "query": "Remind me what I said about my diet.",
        "readable_cube_ids": ["b32d0977-435d-4828-a86f-4f47f8b55bca"],
        "writable_cube_ids": ["b32d0977-435d-4828-a86f-4f47f8b55bca"],
    },
    stream=True,
    headers={"Accept": "text/event-stream"},
)

client = sseclient.SSEClient(response)
full_response = ""
for event in client.events():
    payload = json.loads(event.data)
    if payload["type"] == "text":
        full_response += payload["data"]
        print(payload["data"], end="", flush=True)

print()
```

**SSE 输出示例**

```
data: {"type": "text", "data": "You"}

data: {"type": "text", "data": " told me"}

data: {"type": "text", "data": " you are allergic to nuts"}

data: {"type": "text", "data": " and prefer to avoid gluten."}
```

## 错误响应

| Code | 描述 |
|------|-------------|
| `422` | 验证错误 —— 一个或多个字段未通过类型或约束检查。 |
| `503` | 聊天服务不可用。请使用 `ENABLE_CHAT_API=true` 启动服务器。 |
| `500` | 内部错误。如果错误在流已开始后发生，将作为 `{"type": "error", ...}` 事件而非 HTTP 状态码递送。 |

```json 503
{
  "detail": "Chat service is not available. Chat handler not initialized."
}
```

流内错误如下所示：

```
data: {"type": "error", "content": "Failed to connect to LLM provider."}
```
