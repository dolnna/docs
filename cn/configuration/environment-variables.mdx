---
title: "MemOS 环境变量参考"
description: "用于配置自部署 MemOS 部署的所有环境变量的完整参考，包括 LLM、嵌入器、Neo4j、Redis 和 Chat API 设置。"
---

当您自部署 MemOS 时，所有运行时配置都通过环境变量控制。在项目根目录的 `.env` 文件中设置它们，或在启动服务器之前在 shell 中导出它们。MemOS 不会自动加载 `.env` 文件 —— 请使用 `python-dotenv` 或 Docker Compose 注入它们。

<Note>
  如果您使用 MemOS 云端 API，则无需管理任何这些变量。它们仅适用于自部署部署。
</Note>

---

## LLM（聊天模型）

这些变量配置 MemOS 用于记忆提取、摘要和可选 Chat API 的主要语言模型。

| 变量 | 必需 | 描述 |
|---|---|---|
| `OPENAI_API_KEY` | 是 | 您的聊天模型提供商的 API 密钥。可用于任何兼容 OpenAI 的端点。 |
| `OPENAI_API_BASE` | 是 | Chat Completions 端点的基础 URL（例如 `https://api.openai.com/v1`）。 |
| `MOS_CHAT_MODEL` | 是 | 用于记忆提取、摘要和聊天的默认模型名称（例如 `gpt-4o-mini`、`qwen3-max`）。 |
| `CHAT_MODEL_LIST` | 否 | 通过 Chat API 可用的模型配置 JSON 数组。启用多模型支持和特定于模型的参数，如 `enable_thinking`。 |

**`CHAT_MODEL_LIST` 条目示例：**

```json
[{
  "backend": "qwen",
  "api_base": "https://dashscope.aliyuncs.com/compatible-mode/v1",
  "api_key": "your-api-key",
  "model_name_or_path": "qwen3-max",
  "extra_body": {"enable_thinking": true},
  "support_models": ["qwen3-max"]
}]
```

---

## 嵌入器

嵌入器将记忆和查询转换为向量以进行语义搜索。使用 `general_text` 或 `tree_text` 记忆后端时，所有嵌入器变量都是必需的。

| 变量 | 必需 | 描述 |
|---|---|---|
| `MOS_EMBEDDER_BACKEND` | 是 | 嵌入后端。对兼容 OpenAI 的提供商使用 `universal_api`，对本地 Ollama 服务器使用 `ollama`。 |
| `MOS_EMBEDDER_MODEL` | 是 | 模型名称或路径（例如 `text-embedding-v4`、`nomic-embed-text:latest`）。 |
| `MOS_EMBEDDER_API_BASE` | 是 | 嵌入端点的基础 URL。 |
| `MOS_EMBEDDER_API_KEY` | 是 | 嵌入端点的 API 密钥。Ollama 使用任意非空字符串。 |
| `EMBEDDING_DIMENSION` | 是 | 嵌入模型输出的向量维度。必须与模型完全匹配（例如 `1024`、`768`、`3072`）。 |
| `MOS_RERANKER_BACKEND` | 否 | 在向量检索后使用的重排器。选项：`cosine_local`（默认，无需额外服务）、`http_bge`（外部 BGE 重排器）。 |

---

## Memory Reader

Memory Reader 用于文档摄取和从文件中提取记忆。它可以使用与主聊天模型不同的模型和 API 密钥。

| 变量 | 必需 | 描述 |
|---|---|---|
| `MEMRADER_MODEL` | 否 | Memory Reader LLM 的模型名称（例如 `qwen3-max`）。如果未设置，回退到 `MOS_CHAT_MODEL`。 |
| `MEMRADER_API_KEY` | 否 | Memory Reader 端点的 API 密钥。 |
| `MEMRADER_API_BASE` | 否 | Memory Reader 端点的基础 URL。 |

---

## Neo4j（树状记忆）

当 `MOS_TEXT_MEM_TYPE=tree_text` 时需要 Neo4j。这些变量配置到您的 Neo4j 实例的连接。

| 变量 | 必需 | 描述 |
|---|---|---|
| `NEO4J_URI` | 是（树状记忆） | Neo4j 的连接 URI（例如 `bolt://localhost:7687`）。 |
| `NEO4J_USER` | 是（树状记忆） | Neo4j 用户名（默认：`neo4j`）。 |
| `NEO4J_PASSWORD` | 是（树状记忆） | Neo4j 密码。 |
| `NEO4J_DB_NAME` | 是（树状记忆） | 要使用的数据库名（默认：`neo4j`）。 |
| `NEO4J_BACKEND` | 是（树状记忆） | Neo4j 后端类型。对社区版（推荐）使用 `neo4j-community`，对企业版使用 `neo4j`。 |
| `MOS_NEO4J_SHARED_DB` | 否 | 设置为 `true` 以启用多租户模式，多个用户共享一个 Neo4j 数据库，按 `user_name` 隔离。默认：`false`。 |

<Note>
  对于大多数自部署部署，`neo4j-community` 是推荐的后端。它禁用仅企业版的功能，并使用 Qdrant 进行向量索引，而不是原生 Neo4j 向量索引。
</Note>

---

## 文本记忆类型

| 变量 | 必需 | 默认值 | 描述 |
|---|---|---|---|
| `MOS_TEXT_MEM_TYPE` | 否 | `general_text` | 文本记忆后端。选项：`naive_text` (SQLite，无需额外配置)、`general_text` (Qdrant，生产就绪)、`tree_text` (Neo4j，关系图记忆)。 |

请参阅 [为 MemOS 选择记忆后端](/cn/configuration/memory-backends) 了解每个选项的完整比较和设置指导。

---

## 调度器

MemScheduler 异步处理记忆操作。默认情况下，它使用进程内队列；Redis 启用分布式调度。

| 变量 | 必需 | 默认值 | 描述 |
|---|---|---|---|
| `DEFAULT_USE_REDIS_QUEUE` | 否 | `false` | 设置为 `true` 以为默认调度器启用基于 Redis 的调度。 |
| `MOS_SCHEDULER_USE_REDIS_QUEUE` | 否 | `false` | 设置为 `true` 以专门为 MemOS 调度器启用 Redis。 |

启用 Redis 队列时，请确保有可达的 Redis 实例。默认 Redis 连接是 `localhost:6379`。要将调度器与 Redis 一起使用，请安装额外的依赖组：

```bash
pip install "MemoryOS[mem-scheduler]"
```

---

## Chat API

| 变量 | 必需 | 默认值 | 描述 |
|---|---|---|---|
| `ENABLE_CHAT_API` | 否 | `false` | 设置为 `true` 以启用 `/chat/completions` 端点。需要配置 `CHAT_MODEL_LIST`。 |

---

## 完整的 `.env` 示例

以下是使用通过 DashScope 的 Qwen 和 Neo4j 社区版进行自部署的可工作 `.env`：

```env
# Chat model
OPENAI_API_KEY=your-dashscope-api-key
OPENAI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
MOS_CHAT_MODEL=qwen3-max

# Memory Reader
MEMRADER_MODEL=qwen3-max
MEMRADER_API_KEY=your-dashscope-api-key
MEMRADER_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# Embedder
MOS_EMBEDDER_BACKEND=universal_api
MOS_EMBEDDER_MODEL=text-embedding-v4
MOS_EMBEDDER_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
MOS_EMBEDDER_API_KEY=your-dashscope-api-key
EMBEDDING_DIMENSION=1024
MOS_RERANKER_BACKEND=cosine_local

# Memory backend
MOS_TEXT_MEM_TYPE=tree_text

# Neo4j
NEO4J_BACKEND=neo4j-community
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password
NEO4J_DB_NAME=neo4j
MOS_NEO4J_SHARED_DB=false

# Scheduler
DEFAULT_USE_REDIS_QUEUE=false

# Chat API
ENABLE_CHAT_API=true
CHAT_MODEL_LIST=[{"backend": "qwen", "api_base": "https://dashscope.aliyuncs.com/compatible-mode/v1", "api_key": "your-dashscope-api-key", "model_name_or_path": "qwen3-max", "extra_body": {"enable_thinking": true}, "support_models": ["qwen3-max"]}]
```
